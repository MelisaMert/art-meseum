<%- include('../partials/_header') %> 

<div class="columns is-multiline">
        <% collections.forEach(collection => { %>
            <div class="column is-4">
                <%- include('../partials/_collection-card',{collection: collection}) %> 
            </div>
      <% }) %>
</div>
